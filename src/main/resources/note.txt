这里的把用户的点赞记录存到redis中
结构为key: "thumb:"+userId , field: blogId , value: thumbId + TTL
这里的四个参数分别是用户id 博客id 点赞记录id 过期时间
保存点赞记录id是为了在取消点赞时能便利的去获取到点赞记录的id
数据的过期方式采用的是冷热数据隔离
即在对执行博客点赞时其发布日期在1个月以内就保存到redis以及数据库 若大于一个月就只存数据库
以后再查询时优先查redis并判断是否过期 若过期就从redis中删除防止浪费空间(使用线程池异步删除)

博客在发布时也会存到redis中(除了内容字段)并设置一个月的TTL 用来判断是否是热门博客
结构: "blog:" + blogId , RedisBlog
这里对数据库进行更新之后redis也要同步更新而不是直接删除 因为有的业务是通过redis中有没有数据来进行业务判断的


8.31
用户进行点赞和取消点赞操作时都直接操作数据库 人数过多会导致性能下降 考虑使用redis替换数据库并定时把redis里的数据批量写入到mysql
redis的数据结构(点赞的临时表) Hash  key: thumb:temp:时间    field: {userId}:{blogId}   value: 1(点赞) -1(取消点赞) 0(无操作)
在使用redis统一保存点赞记录再统一刷到数据库之后 redis保存点赞记录对应的结构改为: key: "thumb:"+userId , field: blogId , value: 1


9.2
更改了点赞的逻辑
现在是把每张表点赞数变化记录带着当时点赞的时间临时放在redis中( key 是 thumb:temp:{yyyyMMddHH:mm},field = userId:blogId,value = 净操作数(可 +/- 累加抵消))
批量的从redis刷到mysql(每分钟一次) 避免了每一次点赞都要修改数据库 降低了数据库的写压力
将用户对某个博客的点赞记录也保存在redis中(不删除) 用于查询用户对某个博客的点赞状态
之后通过这个点赞数变化记录将对应的数据存到mysql的点赞记录表
这里的点赞数变化记录会随着每一次刷到数据库之后使用线程池异步删除
这里的用户对某个博客的点赞记录不会删除 以后查询用户是否对博客进行点赞就直接查询redis就可

9.3
从网上抄一个实现HeavyKeeper的代码 实现HotKey的检测
HotKey会把K个热点Key(用户的的点赞记录)缓存在本地的Caffeine里
是用来判断用户是否对超级热点博客进行了点赞
具体的架构:
k = 100（名额数量）
  ↓ 每个名额里塞一个
Node（内部形态：key + count）
  ↓ 对外展示时包装成
Item（公开形态：key + count）
这里本地缓存的结构为 "thumb:3:233"	0（Long）	用户3 对这233博客点过赞又取消了（标记 0）
当用户取消点赞时 不应该从本地缓存中删除 因为他还是热点数据 所以规定value为0的代表没点赞
